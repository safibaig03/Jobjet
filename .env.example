# Optional Environment Variables for Job Board Platform

# Session Secret (optional - defaults to 'jobboard-secret')
SESSION_SECRET=your-custom-session-secret

# Port (optional - defaults to 5000)
PORT=5000

# Node Environment (automatically set)
NODE_ENV=development